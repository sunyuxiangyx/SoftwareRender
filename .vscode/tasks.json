{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "g++ build active file",
      "command": "/usr/bin/g++",
      "args": ["-std=c++14", "-g", "${workspaceFolder}/main.cpp", "${workspaceFolder}/image.cpp", "${workspaceFolder}/model.cpp", "-o", "${workspaceFolder}/a.out"],
      "options": {
        "cwd": "/usr/bin"
      },
      "problemMatcher": ["$gcc"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "编译器: /usr/bin/g++"
    }
  ]
}